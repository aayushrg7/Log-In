{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "brave",
            "request": "launch",
            "name": "Launch Brave against localhost",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
